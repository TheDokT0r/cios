{
  "name": "chatroom",
  "private": "true",
  "scripts": {
    "start": "bun run --filter '*' dev"
  },
  "trustedDependencies": [
    "@parcel/watcher"
  ],
  "workspaces": [
    "packages/*"
  ]
}
